# 项目过程日志（通用版）

> LLM 文本编码工具 · 通用版 · 从「研究专用脚本」到「可复用、可公开工具」的演进记录
> 最后更新：2026-09-21

## 一、项目定位

把「用大模型给文本做编码/归类」从一次性研究，升级成一套**可复用、可质检、可公开**的通用工具。

与源项目（研究专用的政策文本编码工具，未公开）的关系：源项目证明了「这套方法在真实研究里有效」；本通用项目把其中的**脚本层彻底参数化**，让「换一个研究领域只需换 codebook.json、脚本一行不改」成为现实，并配上一份虚构的招聘信息示例手册，做到可以无顾虑地公开。

## 二、技术架构

| 文件 | 职责 |
|------|------|
| `codebook.json` | 结构化编码手册（示例：招聘信息，4 维度 17 字段），`dimensions` 数组声明维度 |
| `code.py` | 编码引擎：按维度 type 驱动渲染 prompt、校验 schema、写 CSV |
| `qa.py` | 质检引擎：Kappa（含 κ 悖论识别）+ 模板效应竖查 |
| `test_smoke.py` | 冒烟测试：26 项离线断言，全部从手册推导、不写死维度名 |
| `.github/workflows/smoke.yml` | CI：每次 push 自动跑冒烟测试 |

参数化的核心：维度用 `type` 声明四种形态，脚本按 type 驱动渲染/校验/Kappa/CSV。

| type | 含义 | 数据形态 |
|------|------|---------|
| `single` | 单选 | `values[].value` 之一 |
| `scale` | 量表 | `scale[].value` 范围内的整数 |
| `multi_group` | 多选（分组） | `groups[].items[].id` 子集 |
| `multi_flat` | 多选（平铺） | `items[].id` 子集 |

`qa` 配置块声明质检字段：`kappa_fields`（信度检验字段，默认全部维度）+ `fingerprint_fields`（模板效应指纹字段，默认非量表维度）。

## 三、开发时间线

| 时间 | 事件 |
|------|------|
| 2026-09-20 | 完成 `codebook.json`（招聘信息示例手册）、`code.py`、`qa.py` 参数化 |
| 2026-09-20 | 完成 `test_smoke.py`（26 项）、README、`config.json.example`、`.gitignore`、LICENSE、3 份示例输入 |
| 2026-09-20 | 冒烟测试 ALL PASS；demo 产出（跑 LLM 3 份 + 手写黄金标准 + 质检报告） |
| 2026-09-21 | git init + 首次 commit + push 上线 GitHub |
| 2026-09-21 | CI 首次跑绿；README 加测试徽章、英文摘要、方法论出处、用法演示 |

## 四、关键决策

### 1. 参数化：dimensions 数组 + type 四类型

源项目是 5 维度硬编码（D1–D5）。通用化时改成 `dimensions` 数组，每个维度声明 `type`，脚本按 type 驱动。四类型恰好覆盖源项目的全部维度形态（D1/D4→single，D2→scale，D3→multi_group，D5→multi_flat）。

### 2. 示例手册选「招聘信息」

公开仓库不能带未发表的研究数据。选一份**虚构的招聘信息编码手册**作演示，既验证「换手册不改代码」，又不触碰任何真实研究。

### 3. demo 产出物：让「能跑」变「有结果可看」

空仓库访客看不到工具长什么样。补上跑 LLM 的编码结果 + 手写黄金标准 + 质检报告，仓库从「能跑」变「有结果可看」。

### 4. CI + 徽章

GitHub Actions 每次 push 自动跑 `test_smoke.py`，README 挂绿色徽章，展示工程能力。

### 5. 用法演示：让访客 30 秒看懂工具

README 里「怎么跑」「怎么配手册」都有，但缺「跑完长什么样」。补一节「用法演示」，贴一段真实输入 → 编码 JSON → Kappa 质检表格，访客不跑代码也能一眼看懂工具的输入输出闭环。

## 五、上线过程与踩坑

| 坑 | 现象 | 解决 |
|------|------|------|
| git 提交邮箱暴露 | 源项目 commit 用过 `mary@local`（假邮箱）和内网 IP | 用 GitHub noreply 邮箱（隐藏真邮箱 + 贡献图绿） |
| Token 缺 workflow 权限 | 仓库含 `.github/workflows/` 时，只勾 repo 的 Token 被拒 | Token 勾 `repo` + `workflow` 两个 scope |
| 终端密码不回显 | `Password:` 提示符下粘贴不可见，误以为卡住 | 认识这是安全设计；或用 URL 内嵌 Token 的方式 push |
| GitHub 网络时断 | 国内直连 github.com 时通时断 | push 期间保持 VPN 开启 |

## 六、一个 demo 里的插曲（恰好证明工具价值）

demo 第一版质检报告里 H2 的 κ=0.000。追查发现：是**人工黄金标准漏标了**——示例 1 任职资格写「熟悉 SQL」，而手册里 H2（数据分析）的触发词明确含「SQL」，LLM 标对了，错在人工标准。修正后 Kappa 全 1.000。

这件事本身就是这个工具存在的理由：漏标、多标靠人眼抽查发现不了，Kappa 一算就现形。

## 七、安全底线

- 真实 API key（`config.json`）从未进 git 历史，`.gitignore` 从 init 起拦截；
- 公开仓库里只有 `config.json.example`（占位符），无任何真实凭证、无任何研究数据。
